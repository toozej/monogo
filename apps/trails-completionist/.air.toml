[build]
cmd = "make local-build local-kill local-run APP=trails-completionist"

include_ext = ["go", "tpl", "tmpl", "html"]

exclude_dir = ["assets", "tmp", "vendor", "testdata", "out", "dist"]

[misc]
clean_on_exit = true
