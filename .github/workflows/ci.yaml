---
name: CI

on:
  pull_request:
  workflow_dispatch:
  push:
    branches:
      - "*"
  schedule:
    - cron: "0 1 * * 1"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    # Repo-wide checks (pre-commit, govulncheck) run once rather than once per
    # app; per-app license reports run in the test matrix. Configs are generated
    # a single time and the `-no-generate`/`-no-prereqs` Make variants avoid
    # redundant regeneration.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.0
      - uses: actions/setup-go@v6
        with:
          go-version: '^1.26'
          check-latest: true
      - name: Install gomplate and lint tools
        run: |
          pip install pre-commit
          make pre-reqs pre-commit-install-no-prereqs
      - name: Generate all app configs
        run: make generate-all
      - name: Run pre-commit and vulnerability checks
        run: make pre-commit-run-no-generate

  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        app:
          - monogo
          - gotts-it
          - url2anki
          - RSSFFS
          - go-find-liquor
          - photos2map
          - go-listen
          - ghreleases2rss
          - terranotate
          - lego-stego
          - files2prompt
          - trails-completionist
          - go-sort-out-gh-actions
          - kmhd2playlist
          - rss2socials
    steps:
      - uses: actions/checkout@v7.0.0
      - uses: actions/setup-go@v6
        with:
          go-version: '^1.26'
          check-latest: true
      - name: Run tests
        run: make test APP=${{ matrix.app }}
      - name: Report licenses
        run: make licenses APP=${{ matrix.app }}

  gitleaks:
    name: gitleaks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.0
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v3.0.0 # nosemgrep: yaml.github-actions.security.third-party-action-not-pinned-to-commit-sha.third-party-action-not-pinned-to-commit-sha
        env:
          # gitleaks-action requires a token to read PR commits on pull_request
          # events; the built-in GITHUB_TOKEN is always available for same-repo PRs.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  codeql-analyze:
    name: codeql-analyze
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@v7.0.0
      - uses: actions/setup-go@v6
        with:
          go-version: '^1.26'
          check-latest: true
      - name: Install gomplate
        run: make pre-reqs
      - name: Generate app configs
        run: make generate-all
      - uses: github/codeql-action/init@v4.36.2 # nosemgrep: yaml.github-actions.security.third-party-action-not-pinned-to-commit-sha.third-party-action-not-pinned-to-commit-sha
        with:
          languages: go
      - uses: github/codeql-action/autobuild@v4.36.2 # nosemgrep: yaml.github-actions.security.third-party-action-not-pinned-to-commit-sha.third-party-action-not-pinned-to-commit-sha
      - uses: github/codeql-action/analyze@v4.36.2 # nosemgrep: yaml.github-actions.security.third-party-action-not-pinned-to-commit-sha.third-party-action-not-pinned-to-commit-sha

  trivy-scan-source:
    name: trivy-scan-source
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.0
      - name: Run Trivy vulnerability scanner in fs mode
        uses: aquasecurity/trivy-action@master # nosemgrep: yaml.github-actions.security.third-party-action-not-pinned-to-commit-sha.third-party-action-not-pinned-to-commit-sha
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'

  snyk:
    name: snyk
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - uses: actions/checkout@v7.0.0
      - name: Run Snyk to check for vulnerabilities
        uses: snyk/actions/golang@master
        continue-on-error: true # To make sure that SARIF upload gets called
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --all-projects --sarif-file-output=snyk.sarif
      - name: Upload result to GitHub Code Scanning
        # Only upload when Snyk actually produced a SARIF (it does not when
        # SNYK_TOKEN is unset), so a missing report does not fail the job.
        if: ${{ hashFiles('snyk.sarif') != '' }}
        uses: github/codeql-action/upload-sarif@v4.36.2
        with:
          sarif_file: snyk.sarif

  workflow-keepalive:
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - uses: liskin/gh-workflow-keepalive@v1
